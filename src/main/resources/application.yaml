spring:
  application:
    name: ofbr-srv-create-consent
  datasource:
    url: jdbc:mysql://localhost:3306/openfinancebrasil
    username: ofuser
    password: ofbr2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
